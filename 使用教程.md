# WARNO Lite Modding Tool 使用教程

适用版本：**1.9.15**。本文说明当前 Windows x64 版的操作；不同 Mod 的可用字段和功能可能不同。

[返回首页](README.md#chinese) · [English guide](USER_GUIDE.md) · [版本更新](RELEASE_NOTES.md#chinese)

## 目录

1. [准备与基本概念](#prepare)
2. [安装、打开或创建 Mod](#open-mod)
3. [界面、搜索与编辑模式](#workspace)
4. [第一次修改：调整一个单位的价格](#first-edit)
5. [草稿、正式应用与备份恢复](#drafts)
6. [单位编辑、批量与创建](#units)
7. [武器与弹药](#weapons-ammo)
8. [战术师、师徽与简介](#divisions)
9. [将军模式与战略 Pack](#strategic)
10. [游戏规则、经验与地形](#rules)
11. [专业工具与引用检查](#professional)
12. [创建、生成、开发模式与上传](#mod-tools)
13. [设置、常见问题与反馈](#help)
14. [当前使用边界](#limits)

<a id="prepare"></a>

## 1. 准备与基本概念

你需要完整解压的工具发布包，以及一个可编辑的 WARNO Mod 源目录。原版名称、官方图片、原版师简介和官方创建/生成流程还需要本机安装的 WARNO。工具本身不要求安装 Python、.NET SDK 或解包软件，也不要求放在游戏目录里。

先分清三个目录：

| 目录 | 如何识别 | 什么时候选择 |
| --- | --- | --- |
| 编辑器目录 | 有 `WarnoLiteModdingTool.exe` 及随包文件 | 启动工具 |
| WARNO 游戏目录 | 游戏安装位置，包含 `Data/PC` | 设置原版名称、图片和正文来源 |
| 目标 Mod 根目录 | 包含该 Mod 的 `GameData`，完整官方项目还包括生成脚本等 | 打开和编辑 Mod |

例如目标目录是 `MyMod/GameData/...`，打开时选 `MyMod`，不要继续选到 `GameData` 内部。只有生成产物、没有可编辑源文件的目录不能代替 Mod 源目录。

编辑过程有三个独立阶段：

| 阶段 | 做了什么 | 游戏是否已经使用修改 |
| --- | --- | --- |
| 保存草稿 | 保存待修改内容，正式源文件保持原样 | 没有 |
| 预览并应用 | 校验、备份并写入当前 Mod 的正式源文件 | 还需要生成 |
| 生成 / 编译 | 调用 WARNO 官方流程处理正式源文件 | 生成成功后，仍需在游戏中启用并检查 |

首次练习可使用专门的测试 Mod。工具的事务备份记录应用前的相关文件，不代替你对整个项目的长期备份。

<a id="open-mod"></a>

## 2. 安装、打开或创建 Mod

### 2.1 下载与启动

1. 前往[项目发布页](https://github.com/USN-Enterprise/WARNO-Lite-Modding-Tool/releases/latest)，选择 Windows x64 便携 ZIP；普通用户不需要下载 Source code。
2. 将整个 ZIP 解压到本机目录。不要在压缩包内运行，也不要只复制 EXE。
3. 运行 `WarnoLiteModdingTool.exe`。同一 Windows 登录会话只允许一个实例；提示“已在运行”时返回已有窗口。
4. 升级前先正常退出旧版，建议把新版本解压到新目录。草稿和事务备份在目标 Mod 内，不在编辑器安装目录里。

本文针对 1.9.15；下载页可能已有后续版本，请结合相应发布说明阅读。

### 2.2 打开已有 Mod

1. 点击“自动查找 Mod”，选择检测结果中的目标项目；“Mod 工具”中也有同名入口。
2. 未找到时，使用“打开 Mod 文件夹”，手动选择包含 `GameData` 的 Mod 根目录。
3. 等待加载，核对当前项目路径和对象列表，避免打开同名项目的另一份副本。
4. 若名称只显示内部标识，按[原版名称设置](#help)指定游戏目录。可用参数通常仍能编辑。

功能按目标 Mod 实际文件开放。只有弹药或规则文件的项目也可以使用相应功能；不需要为它补齐无关模块。

### 2.3 还没有 Mod：通过工具创建

1. 点击欢迎页或“Mod 工具”中的“创建新 Mod”。
2. 选择 **WARNO 的 `Mods` 父目录**。该目录需包含官方创建脚本和 `ModData/base.zip`；此处与打开已有 Mod 时选择的目录不同。
3. 输入 1–32 位英文字母或数字组成的名称，例如 `MyFirstMod`，不能与现有 Mod 重名。
4. 核对目标位置后确认创建，等待官方流程结束。创建成功后工具会尝试打开新项目。
5. 若创建完成但打开失败，保留创建输出，使用“打开 Mod 文件夹”重试。若提示脚本或基础包缺失，先检查所选目录。

<a id="workspace"></a>

## 3. 界面、搜索与编辑模式

左侧选择模块，列表中选择对象，检查器显示其字段。顶栏提供“草稿总览”“生成 / 编译 Mod”和“问题”等入口，左下角可打开设置和切换专业模式。

- **搜索**：优先输入显示名称；也可尝试内部标识的一部分。候选取自当前 Mod。
- **筛选**：按阵营、国家、类别、栏位、角色或草稿状态等缩小范围，各模块条件不同。单位筛选可选“全部条件 / 任一条件”。
- **选择与勾选**：点击一行是浏览对象；复选框是加入批量范围。二者不要混淆。
- **全选当前筛选**：处理完整筛选结果，不只处理屏幕上看得见的几行。重新筛选不等于清空原有勾选；批量操作前核对已选数和隐藏选择。
- **布局**：拖动分隔条调整面板宽度；列表和侧栏可滚动。独立弹出面板见[设置](#help)。

本文把常用编辑视图称为“基础模式”；当前设置的选项仍可能显示为“普通模式”，英文为 `Standard mode`。它们指同一模式。勾选“专业模式”后会增加复杂公式、索引、来源及部分专业字段；切换模式不会丢弃已保存草稿。

字段旁的原参数名用于辨认对应数据。只读或不可用项应先看原因，不能靠切换专业模式绕过缺失资料或结构冲突。

<a id="first-edit"></a>

## 4. 第一次修改：调整一个单位的价格

这一练习只修改一个已有单位的部署价格。以你当前项目显示的数值为准，不需要找到特定国家或型号。

1. 打开目标 Mod，进入“单位”，点击“清空选择”确保不在多选批量状态。
2. 搜索一个准备测试的单位，点击其名称，核对右侧对象。
3. 展开“费用与部署”，找到“指挥点”（原参数 `Resource_CommandPoints`）。这是部署价格；旁边“将军模式价格”是不同字段。
4. 记下原值，输入一个不同的合法整数。例如原值若为 `100`，可以试改为 `90`。这只是示例，不代表任何原版单位的价格。
5. 等待字段草稿保存，查看字段状态。出现输入错误或保存失败时先处理；仅在输入框看到新数字不代表保存成功。
6. 打开“草稿总览”，找到该单位的价格修改，确认原值、目标值和对象正确。若还有其他草稿，可用“应用选中项”限定本次范围；“预览并应用全部”会处理项目中的全部草稿。
7. 等待预览完成，检查修改清单和关联影响，再确认应用。完成后查看结果与备份信息。
8. 点击“生成 / 编译 Mod”，阅读确认提示并执行。等待完成，查看输出是否成功；失败时按[排错表](#help)处理。
9. 在游戏中启用相应 Mod，选择实际包含该单位的师或牌组，核对显示价格。游戏中找不到单位时先确认使用的是同一 Mod 及正确的师/牌组。

还没应用时可以“撤销此项”或删除该草稿；已经应用时可以把价格改回原值并再次应用，或按[备份恢复](#drafts)处理。正式文件改变后，要再次生成才能检查游戏结果。

<a id="drafts"></a>

## 5. 草稿、正式应用与备份恢复

### 5.1 哪些操作需要点击保存

普通单字段编辑通常自动保存草稿。单位/弹药批量、创建向导、师名称与徽章、师简介、战略 Pack 等操作有明确的“加入草稿”按钮；单位图片和特性与实际能力窗口使用“保存草稿”。按各窗口按钮完成操作，再到草稿总览确认。

草稿保存在 `<Mod>/.warno-editor/draft-v1.json`。切换项目或正常关闭前，程序会等待已接入自动保存流程的编辑完成；保存失败会保留项目和输入。带手动保存按钮的页面，应先完成保存，不能把关闭窗口当成“加入草稿”。

### 5.2 查看与应用

1. 打开“草稿总览”，按修改摘要、模块、状态和源文件找到内容。
2. 选择条目，查看原值 → 目标值、对象与作用域。批量内容可展开查看子项。
3. 使用“应用选中项”或“预览并应用全部”。部分应用仍须满足关联依赖，例如创建对象及必要引用不能拆坏。
4. 等待候选构建和校验结束，阅读最终预览。专业模式可进一步查看按文件组织的修改前后源码。
5. 确认后才写正式文件。外部文件变化、引用变化或候选校验失败会阻止相关提交，应依据诊断处理并重新预览。

“删除选中项”或“清空全部草稿”取消的是未应用修改；它们不能撤销已经写入的正式文件。若提示正式提交成功但刷新失败，请重新打开当前 Mod 检查结果，不要直接再次应用同一修改。

### 5.3 恢复已应用修改

每次正式应用会在 `<Mod>/.warno-editor/backups/<备份编号>/` 保存相关原件和事务清单，并向该 Mod 的 `logs/` 写入记录。

1. 在“草稿总览”展开“事务备份与恢复”，找到对应应用记录。
2. 点击“恢复”，阅读将恢复的文件清单。
3. **恢复按文件进行**，会恢复到该次应用之前的状态；同一文件中后来产生的其他修改也可能被覆盖。先核对清单，不要把它理解成只撤销某一个输入框。
4. 确认后程序先备份恢复前状态，再执行恢复；查看完成或失败信息。
5. 检查重新显示的数据和剩余草稿状态，需要游戏生效时重新生成。

新建单位的来源核对还会使用创建备份。仍需编辑或删除这些单位时，不要随意清理其创建备份。备份用于恢复相关文件，不承诺恢复完整游戏安装或所有外部手工操作。

<a id="units"></a>

## 6. 单位编辑、批量与创建

### 6.1 数值、名称与经验类型

选中单位后按分组修改可编辑字段：费用、部署、生存、视野、机动及其他已识别参数。输入框、下拉和标签选择各有用途，未知既有值会保留。

- 名称编辑与内部变量名不同。共用名称 token 的单位可以独立改名；创建或改名需先加载本机名称词典，完成冲突检查。
- ECM 用正百分比显示；不要照搬原 NDF 的符号。基础视野输入会按原比例联动相关视野值；在预览中检查组合结果。
- 前置部署可填非负距离，也可选择当前 Mod 的已有档位。参考档位不是对所有单位自动生效的能力配套。
- “经验类型”选择当前 Mod 的可用路线，展开“各等级效果”查看。给单位换路线与修改共享路线数值是两个操作，后者在[游戏规则](#rules)中进行。

### 6.2 特性与实际能力

1. 点击“特性与实际能力”。
2. 基础模式选择已提供的 15 类配套，结合当前 Mod 说明确认角色与效果；同一图标可能有不同能力分支。
3. 专业模式可搜索当前 Mod 可解析的已有能力，添加、替换、移除或清空；显示标签可以单独编辑。
4. 在窗口保存草稿，核对预览后应用。

只改显示标签并不等于获得实际能力。此入口主要管理单位的能力引用和显示配套，不提供任意共享能力强度编辑；未知或多义结构会限制写入。

### 6.3 单位批量编辑

1. 勾选目标单位。至少两个单位被勾选时，右侧自动切为批量检查器；也可点击“批量编辑”。
2. 选择“已勾选 Unit”或“当前筛选结果”等对应范围，核对数量。
3. 共有字段可以统一设值。不同原值显示为混合状态，空输入不表示要清零；填入目标值后再加入草稿。
4. 公式编辑选择字段和操作。基础模式提供固定值、增减百分比；专业模式增加乘法、固定加减、取整和上下限。
5. 先预览目标、结果及关联影响，再“加入草稿”。例如增加 10% 表示原值乘 `1.1`，不是统一加 `10`；整数结果还要按所选规则取整。
6. 最终按[通用应用流程](#drafts)写入。清空勾选并退出批量编辑可返回单对象检查器。

### 6.4 从模板新建单位

点击“新增单位”，依次完成五页：

1. **选择母版**：选当前 Mod 的已有单位，作为字段、模型、动画和槽位来源。
2. **基本设置**：填写显示名称，调整提供的字段及能力。变量名默认按母版递增建议；专业模式可以修改。
3. **武器配置**：在固定槽位选择已有 Ammo，可恢复母版。改动的 Weapon 会自动隔离；需要整体独立武器配置时选择对应选项。
4. **可用师**：勾选所需战术师，设置提供的卡片参数和运输候选；检查当前选中行后再调整运输。
5. **创建草稿**：查看汇总，点击“加入草稿”。此时尚未写入正式数据。

待创建单位可通过“编辑创建设置”继续调整。正式应用会一并写入身份、名称、注册及所选师规则。新单位沿用母版外观，替换 Ammo 不会自动适配新模型或发射动画。

### 6.5 变量名、注册与删除

- 专业模式的“修改单位变量名”修改内部标识并联动可确认引用，不等于修改显示名称；现有 GUID、名称 token 和注册编号保持各自职责。
- “检查单位注册”用于检查并在可确认时生成定向修复草稿；先阅读问题与修改清单，不需要无故对每个单位重复操作。
- “删除新建单位 / 撤销删除”可以取消未应用创建。删除已应用单位时，必须核对它由本工具在当前项目创建的备份与身份。
- 删除预览列出引用；可安全清理的师规则和运输候选联动处理，其他使用者需要先选择替代或解除引用。共享 Weapon/Ammo 等资源保留。
- 正式删除前可撤销待删除状态，应用后通过文件级备份恢复。不支持把此入口当作删除任意原版单位的通用工具。

### 6.6 单位卡片图片

1. 选中单位，点击“修改单位图片”。可以选择当前 Mod 已有图片，也可“导入自定义图片”。
2. 在独立图片窗口裁剪、缩放查看或处理圆形区域透明度，检查浅色/深色背景下的结果。
3. 确认图片编辑结果后，在单位图片窗口点击“保存草稿”。重新打开可查看待应用结果。
4. 预览并应用，再生成 Mod。自定义图片使用独立 PNG 与纹理键，仅重定向所选单位，原图保留。

输入 PNG/JPEG 不超过 8 MB，边长不超过 4096 像素；输出 PNG 的草稿编码还有大小校验，失败时缩小图片后重试。这里只改变卡片图片，不改变 3D 模型、涂装或动画。

<a id="weapons-ammo"></a>

## 7. 武器与弹药

### 7.1 先选择正确入口

| 目的 | 入口与影响 |
| --- | --- |
| 只增强某一个单位的武器 | “武器”选择当前 Unit 作用域，必要引用会隔离 |
| 对若干单位修改同类武器参数 | “武器”选择所选 Unit 范围，核对全部目标 |
| 改一种弹药，让所有使用者采用新值 | “弹药”直接编辑共享 Ammo，并查看使用者 |
| 一次调整多种弹药 | “弹药”勾选后批量编辑；每种弹药的所有使用者都受影响 |

弹药列表上的国家、类型或其他筛选只限制选中的弹药对象，**不会把共享弹药影响限制在筛选出来的单位**。

### 7.2 武器页：挂载与局部参数

1. 搜索并选中 Unit，再选择需要编辑的 Weapon/挂载。
2. 修改前确认作用域：默认仅当前 Unit，也可选多个 Unit；专业模式另有全部引用范围。
3. 修改当前可解析的库存/齐射、武器数量 `NbWeapons`、隐藏开关、炮塔射界，或选择已有 Ammo 替换。
4. 调整挂载所用 Ammo 的可编辑参数。局部修改时工具按需复制最小 `Ammo → Weapon → Unit` 引用链。
5. 检查草稿预览中的共享、同弹药箱或同炮塔关联影响，再应用。

界面显示弹药箱索引、齐射次数、每齐射射弹数及可推导总弹量；不要把这些数值当成同一个概念。表现链中的 EffectTag、动画键等用于参考，不能自由新增/删除武器槽，也没有整套 Weapon 替换选择器。

### 7.3 弹药页：共享本体与批量

单条编辑：搜索并选择 Ammo，调整可编辑的名称、射程、伤害、压制、溅射、精度、散布、射击、弹道、补给和行为字段；展开“查看引用”检查 Ammo → Weapon → Unit 链。修改或改名均影响全部引用者。

批量编辑：

1. 勾选多条弹药，或打开批量编辑；选择“已勾选弹药”或“当前筛选结果”。浏览另一行不改变勾选。
2. 核对隐藏的已选数量。共有字段可统一设值，混合值留空等待输入。
3. 使用固定值、百分比或专业公式。公式逐条按有效当前值计算；整数明确选择取整，小数默认保留小数。
4. 预览完整逐行结果及可搜索使用者列表，点击“加入草稿”。
5. 在草稿总览预览并应用，检查是否有局部同字段修改或替换 Ammo 的关联冲突。

未被使用的 Ammo 及仅有弹药文件的项目也可编辑。旧“批量修改武器”独立窗口入口已撤下；已有历史批量草稿仍可在草稿中心应用或移除。不要按旧版教程寻找该窗口。

<a id="divisions"></a>

## 8. 战术师、师徽与简介

### 8.1 单位池与费用

1. 在“战术师”搜索并选择一个师，可用国家、阵营、类型和草稿状态等筛选。
2. 在“单位池”使用“添加 Unit”选择单位；可按名称或内部标识搜索，再按国家、阵营、栏位、角色筛选。
3. 选中规则行，调整最大卡数、单卡数量、出场老练度相关设置及运输候选。运输支持多选，完成后点“应用选择”；这一步只确认选择，正式文件仍须应用草稿。
4. 在“师级设置与费用”调整类型、标签、特色单位和费用曲线。基础模式按类别提供十个数值格，额外尾部项在专业模式处理。
5. 到草稿总览检查该师的组合结果并应用。

新增运输候选要求具有唯一可识别的运输模块。既有但不能确认的兼容值会保留并标注。删除单位池规则时，若单位仍被只读默认卡组引用，操作会被阻止。

### 8.2 新建师与改名

点击“新建战术师”，选择模板、名称及师徽，加入草稿；再次打开此入口可选择“继续编辑待创建师”。应用后，再在普通师页面修改新师单位池和费用。

新师创建会自动生成独立的规则、费用和默认牌组数据；**这不等于开放默认卡组编辑器**。现有默认卡组保持只读。已有师的名称与徽章从“名称与徽章”编辑。

### 8.3 师徽图片与固定模板

1. 在创建师窗口或“名称与徽章”中选择“选择已有师徽”，搜索缩略图库；也可点“导入或编辑图片”导入 PNG/JPEG。
2. 图片窗口支持缩放查看、矩形裁剪、圆形内/外透明、撤销与重置；确认编辑结果后返回师设置。
3. 需要番号师徽时，选择“固定模板生成”：现有十种模板覆盖近卫、东德、空降、黑盾、波兰四种几何和捷克两种中央符号，支持 0–3 位番号；捷克提供四种配色，波兰伞锚不在模板中。
4. 在师设置窗口点击“加入草稿”，再预览、应用并生成。

固定模板含重构底图，不保证逐像素还原原版。自定义 PNG、纹理声明与目标师引用一起提交，原图与其他使用者保留。单位卡片图片使用独立入口，不套用师徽模板。

### 8.4 师简介与历史正文

1. 选中已有师，进入“师简介”；新建师先应用创建，再修改正文。
2. 若原文不可用，选择游戏目录，点击“从本机提取 / 刷新原版简介”，等待提取完成；需要时可取消。
3. 切换中英文原版参考语言，展开“原版参考与token”查看。切换参考语言不会覆盖已输入内容；要复制原文到编辑区，点击“采用当前参考文字”。
4. 按分段编辑游戏玩法与历史文字，点击“加入草稿”。“撤销本页输入”用于放弃本页尚未保存的输入。
5. 预览、应用并生成。自定义正文使用独立引用，其他师原文保留。

自定义正文是各语言共用的默认文本，不自动翻译，也不自动继承旧 token 的其他语言译文。“特色单位”在“师级设置与费用”里修改，不是正文输入的一部分。

<a id="strategic"></a>

## 9. 将军模式与战略 Pack

### 9.1 将军模式：编辑现有编制

1. 进入“将军模式”，搜索并选择已有战略营。
2. 展开营/团 → 连 → 排/组 → 单位的编制树，选中要修改的层级。
3. 使用该层级提供的新增、删除、移动、排序、改名或 HQ 操作；在单位项修改单位、数量、老练度和运输。
4. 在“棋子属性”编辑当前对象已识别的行动点、恢复、移动、角色、控制区、支援范围、战略影响、名称或已有图标引用。
5. 查看草稿中的具体编制差异后应用；应用时工具重算必要 PackIndex，并按目标隔离共享定义。

这里编辑的是现有营内部结构，不提供创建整个战略营或编辑战役地图、事件和剧情。未识别字段不会猜测写入。

### 9.2 战略 Pack（SP）：编辑共享组合

Pack 表示战略编制中引用的单位、运输、老练度等组合。独立 SP 页修改共享 Pack 本体，影响所有引用它的编制；与将军模式针对当前编制隔离修改的含义不同。

1. 进入“战略 Pack（SP）”，搜索名称、单位或运输并选择条目。窄窗口可用“查看详情 / 返回列表”切换。
2. 调整单位、运输或“无运输”、老练度以及 Pack 名称；“生成可读名称”提供命名建议，仍需保存。
3. 查看“使用位置”和影响说明。选中使用位置可“跳转到将军模式”，也可双击引用行。
4. 点击“加入草稿”，检查共享影响后统一应用。Pack 改名会同步可确认引用并检查冲突。
5. 有未加入草稿的编辑时切换 Pack，按需要选择保存并切换、放弃并切换或取消。

编制数量与索引继续在将军模式处理。资料不完整时，使用者统计只覆盖已识别文件，不代表已穷尽所有引用。

<a id="rules"></a>

## 10. 游戏规则、经验与地形

进入“游戏规则”，使用搜索及大类/小类折叠查找设置。每个规则的说明、原参数和可编辑性取决于当前 Mod；修改共享规则可能影响双方符合条件的单位。

### 10.1 一般规则与空军布局

一般规则按对局、经济、AI、战斗行为、后勤、将军模式、空军组织。选定字段后阅读说明、修改值、确认草稿，再检查关联预览。参战名额等设置可能需要联动相关表长，不能只看一个数字。

空军布局提供网格方案及卡片倍率：

1. 选择所需行列方案，数量按行数 × 列数确定。
2. 设置卡片倍率，等待自动保存草稿，查看关联布局预览后应用。
3. 倍率以当前正式文件卡片尺寸为基准；重复预览不会累乘。应用并重载后，新尺寸成为 `1×` 基准，再应用倍率会在新尺寸上计算。
4. 连续起飞间隔、撤离开火和撤离高度在专业模式中显示。生成后在游戏里检查实际排布与效果。

### 10.2 经验路线的等级与效果

1. 展开“经验与老练度” → 经验路线 → 等级。
2. 检查该路线使用者，确认你需要改变共享路线，而不是只给某单位换路线。
3. 修改已有等级门槛和支持的效果数值，等待自动保存草稿。
4. 预览全部引用影响及关联文件，再应用。

现有路线的等级结构可能不同。缺失、未知或多处共享而暂不支持编辑的效果保持只读；不自动补建等级、不创建单位独立路线，也不自动改游戏内提示文字。

### 10.3 地形规则

基础模式针对已识别的现有步兵伤害组合提供减伤百分比；专业模式可编辑现有承伤倍率以及隐蔽、视线、通行、航空和可燃性参数。

1. 搜索并选择需要的地形/组合，先阅读说明及影响范围。
2. 修改当前可编辑项；百分比和倍率按界面语义输入，不能混用。
3. 在草稿中检查前后值，再应用和生成。

改动会影响当前 Mod 中双方符合条件的单位。地形高度只读，不开放地形定义或伤害组合增删，也不会把缺失组合自动补成零。仅有地形规则文件的项目可以使用相关功能。

<a id="professional"></a>

## 11. 专业工具与引用检查

勾选“专业模式”，可以查看更多原值、字段路径、文件位置和来源，并使用“引用工作台”。

1. 在对象索引或工作台中搜索对象及字段。
2. 查看引用关系并跳转，确认谁使用当前对象；默认对象排序为源文件、行号，再按字符位置稳定排列，可点击表头调整。
3. 对开放的受控数值字段进行编辑，查看草稿和按文件分页的前后源码。
4. 依照通用流程应用。原始源码预览、身份信息、引用索引或未支持的字段可能只读，不是任意文本编辑器。

英文界面翻译工具自身界面；用户名称、内部标识、外部工具输出及未映射诊断可能仍显示原文。

<a id="mod-tools"></a>

## 12. 创建、生成、开发模式与上传

| 操作 | 入口 | 作用与前提 |
| --- | --- | --- |
| 创建 Mod | 欢迎页 / Mod 工具 → 创建新 Mod | 运行官方创建脚本；选择 Mods 父目录，见[创建步骤](#open-mod) |
| 生成 / 编译 | 顶栏 | 处理当前 Mod 的正式源文件；未应用草稿不参与生成 |
| 启动开发模式 | Mod 工具 | 按当前项目可用的官方入口启动调试流程 |
| 上传 Mod | Mod 工具 | 调用当前项目的官方上传流程，读取确认提示及完整输出 |

各操作分别检测所需脚本和环境；一个入口不可用不等于普通参数编辑不可用。编辑器自身无需外部 Python；主动调用的官方 BAT 使用游戏自带的工具环境。

执行生成前核对项目路径并处理要生效的草稿；执行后查看退出结果与输出。上传可能同时调用官方备份，不能只看到备份信息就认为上传完成。编辑器的事务备份与官方流程备份有不同职责。

游戏更新后，若官方脚本或文件结构发生变化，按当前游戏提供的流程处理。本教程没有替你运行创建、生成、游戏或上传，也不把文件写入成功等同于游戏效果已验证。

<a id="help"></a>

## 13. 设置、常见问题与反馈

### 13.1 原版名称、图片与师简介

打开“设置 → 常规 → 原版名称”，选择含 `Data/PC` 的 WARNO 安装目录，必要时点击“刷新名称”。关闭设置后执行刷新并重载当前项目，已有草稿会先保存。

当前 Mod 的 CSV 名称优先；没有词典时显示内部标识并保留普通参数编辑，创建/改名需先完成词典加载和冲突检查。名称缓存位于 `%LOCALAPPDATA%/WarnoLiteModdingTool/names/cache-v1.json`，官方图片缓存位于 `%LOCALAPPDATA%/WarnoLiteModdingTool/images/v1/`；它们与 Mod 草稿不同，不应互相替代。

单位图片优先读取当前 Mod 自定义 PNG，其余官方图片来自本机游戏。缺图一般不妨碍数值编辑。师简介的提取/刷新在“师简介”页单独操作。

### 13.2 外观、面板与加载缓存

- 语言可选跟随系统、简体中文和 English；默认编辑模式在“常规”设置。
- “外观”提供七套主题及自定义背景；首次无有效主题时默认白蓝，可设置背景透明度和布局。
- “常规 → 允许面板独立弹出”默认关闭。开启后选择大面板或小折叠分组，双击支持的标题弹出，关闭弹窗后归位。师徽编辑器本来就是独立窗口。
- “缓存上次打开的 Mod，加快再次打开”默认开启，可关闭或“清除 Mod 缓存”，设置影响下次打开。源文件变化、程序更新或缓存损坏时重新加载；名称缓存是另一套数据。
- 1.9.15 升级后旧缓存会重建一次。缓存只加快浏览加载，正式预览和提交仍重新核对文件。

### 13.3 常见问题

| 现象 | 建议处理 |
| --- | --- |
| 没找到 Mod | 手动选择包含 GameData 的根目录，核对是否只有生成产物 |
| 模块或按钮不可用 | 查看原因/工具提示及“问题”；检查相关源文件或官方脚本，不从别的 Mod 随意补文件 |
| 只显示内部名、无法改名 | 指定 WARNO 目录，等待名称加载完成；当前 Mod 的自定义 CSV 仍优先 |
| 图片或师简介为空 | 检查游戏目录、目标引用及提取状态；师正文从其页面刷新 |
| 输入了值，草稿里没有 | 查看输入错误/保存状态；批量、SP、师正文和窗口操作须完成“加入草稿”或“保存草稿” |
| 点一行仍在批量界面 | 检查是否仍勾选多个对象，清空勾选并关闭手动批量编辑 |
| 修改影响了其他单位 | 检查是否从共享弹药/SP/规则入口编辑，或选了全部引用；按预览和备份处理 |
| 预览提示冲突 | 查看源文件、引用或关联草稿变化；处理后重新预览，不反复强行确认 |
| 已提交但刷新失败 | 重新打开同一 Mod 核对结果，避免重复应用 |
| 草稿保存失败 | 保留当前输入，查看路径权限、磁盘空间和错误详情，修正后重试 |
| 生成失败 | 先看完整官方输出中的具体错误，核对路径与源文件问题；这与工具草稿保存是不同阶段 |
| 游戏内没有变化 | 确认草稿已应用、生成成功、启用了正确 Mod，并检查正确对象及场景 |

### 13.4 反馈问题

打开顶栏“问题”。“Mod 问题”涵盖项目、解析、草稿/事务与官方流程；“工具问题”涵盖程序自身异常。复制带错误编号的完整诊断，必要时打开日志目录。忽略问题只影响展示，不会绕过应用校验。

向 **QQ 群 1013181135** 反馈时，提供工具版本、所用模块、从哪一步开始出现问题、错误编号/完整诊断和可复现步骤。涉及共享修改时说明选择的作用域；生成问题附官方输出。截图有助于说明界面位置，但不能代替文字诊断。

<a id="limits"></a>

## 14. 当前使用边界

- 面向结构兼容的当前 Mod，不保证任意未知 NDF 结构都能安全写入。缺失、歧义或冲突结构会保留原文并限制对应功能。
- 单位和战术师可以从模板创建；不支持空白单位创建、跨 Mod 单位导入、任意原有单位删除或整师删除。
- 默认卡组没有独立编辑页；模板创建所需的自动注册不等于可自由编辑默认卡组。
- 不自由增删武器槽，不制作模型、动画或涂装。图片编辑范围是单位卡片和师徽 PNG。
- 将军模式编辑现有营内部编制及棋子属性，不创建整营或修改战役地图、事件与剧情。
- 不创建单位独立经验路线，不扩展等级上限，不自动翻译自定义正文或更新游戏提示文字；地形高度只读，地形及组合不支持增删。
- 不保存跨 Mod 批量预设。草稿、备份与应用对象属于各自项目，不能拿其他 Mod 的资料当作默认补全来源。
- 发布目标为 Windows x64 自包含程序；最低 Windows 版本与真实跨屏 DPI 仍有待进一步验证。自定义图片、空军布局、武器数量及部分新增规则的生成/游戏内效果尚有未验范围；各版本具体记录见[发布说明](RELEASE_NOTES.md#chinese)。

[返回目录](#目录) · [返回首页](README.md#chinese) · [English guide](USER_GUIDE.md)
