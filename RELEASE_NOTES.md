# WARNO Lite Modding Tool 发布说明

## 1.8.5 — 2026-09-08

- 修正 Constantes 合法顶层数值、布尔和表达式被误判为缺少左圆括号的问题；继续检查真正的构造器和常量括号错误。只读样本复扫由 13 条诊断降为 0，对象数保持 26,619。
- 去掉“基础模式 32 项 · 专业模式 50 项”界面说明，实际可编辑范围保持。
- 单位、武器、弹药、游戏规则、战术师、将军模式及新增单位的修改项旁显示原参数小字备注；基础与专业、中英文均可见。必要时附 MAP 键、方向或联动参数；原参数标识不翻译。
- 已保存的历史诊断不会自动删除，可在问题中心选择忽略旧误报。
- 验证：70/70 回归测试通过；中英文参数备注、规则页面和相关窗口截图检查通过。未修改真实 Mod 数据、运行生成器或启动游戏。

## 1.8.4 — 2026-09-08

- 基础模式隐藏压制承受、眩晕承受和护甲类型；专业模式保留一个统一护甲类型选择器，四向护甲数值仍分别编辑。
- 基础模式的视野上限、观察强度各保留标准项；低空和高空按正式文件原始比例联动并四舍五入。原始标准值为零或引用值时引导专业模式分别编辑。侦察门槛可选 0 / 1768 / 2651 / 3535 / 5301 / 7068，保留自定义数值和折叠参考，不联动识别率、周期或视野上限。
- 单位特性 SpecialtiesList 使用可搜索、可增删标签；候选随项目后台索引缓存。专业模式可填写自定义标识，允许清空；仅在唯一已有 UI 模块内补建缺失字段，不自动配置运输、烟幕、武器或 TagSet。
- 新增“游戏规则”：基础模式显示已确认编号 1–16、33–48 共 32 项，专业模式显示 1–50 共 50 项。读取当前 Mod 的 Constantes 文件，数值格、数组格和布尔选择器生成草稿；缺失项独立禁用。
- 初始资金新增取值补充歼灭映射；默认选项和索引校验。参战名额变化联动四张数量表长度，新增格继承末格值。数组／MAP 的键和位置不可随意增删或改写。
- 专业工作台只开放已识别数值和获准数值格；源码、文本、布尔、引用、枚举、身份编号和引用索引保持锁定。编辑走原有草稿与事务。
- 中英文主流程同步；规则草稿支持恢复、撤销、删除、部分应用与事务失败回滚。自包含 Windows x64 ZIP，解压即用。

## 1.8.3 — 2026-09-08

- 高级模式更名为专业模式，同步模式开关、设置与提示；英文对应 Professional mode。功能和设置键不变。

## 1.8.2 — 2026-09-08

- 弹药增加游戏内名称编辑。改名自动新建唯一 10 字符 token，UNITS.csv 追加名称并更新当前 Ammo.Name，旧条目保留；弹药页改名影响该弹药的全部使用者。草稿支持重编辑、撤销、恢复和部分应用，名称与引用同事务提交。
- 弹药名称优先使用当前 Mod CSV，其次内置原版中英文词典；打开项目后台解析，列表、挂载及替换候选使用缓存，支持名称、内部标识及 token 搜索。不需要运行时解包工具。
- 将军模式筛选改为多选标签；所属师、战斗角色默认分别折叠，可搜索；已选标签在收起后保留摘要，可单独取消。大列表虚拟化，摘要最多直接展示 12 个标签，其余通过数量及取消全选管理。
- 将军列表增加右侧国家列；普通中文模式显示易懂国名，高级及英文保留代码，未知国家原样保留。沿用华约→北约→未知、国家、自然名称顺序。
- 将军草稿摘要显示编制、国家与修改数，详情及应用确认列出真实前后变化，原始 JSON 仅高级模式；整个编制仍作为一个事务。
- 修复弹药搜索框与筛选重叠、名称列过窄；挂载替换仍保存内部引用，不把显示名写成对象路径。

已安装原版词典重新提取核对后与现有快照一致；研究样本中仍有 26 处 Name 未命中，显示内部标识作为回退，允许自行改名，不宣称原版词条全覆盖。

## 1.8.1 — 2026-09-08

- 多个单位共用名称 token 时允许独立改名，自动新增 10 字符 token 与名称条目，只更新当前单位引用，原名称保留。两份改名草稿可以分开应用。
- 战术师列表默认按国家代码排序，同国家连续排列，保留同国家内原顺序与搜索。
- 切换单位仅展开最后手动展开的分区，其他分区收起；同单位编辑刷新保留当前展开状态，不增加跨重启保存。


## 1.8.0 — 2026-09-08

- 新增单位 B 向导与整体创建草稿，自动分配身份、名称 token、牌组编号；可独立 Weapon 并加入所选师，整链事务/失败回滚。
- 将军按阵营、国家、自然名称排序并增加筛选；战术师搜索、师内筛选和卡片参数编辑；武器/弹药按已缓存的单位引用筛选。
- 普通模式护甲类型四合一，数值仍四向独立；升级来源移至高级模式；中文行为、装填时间、费用格按已确认名称调整。
- 特色单位及标签支持搜索、多选；师标签分三组单选。无需先预览也可加入批量草稿。
- 问题中心双语误报说明、多选忽略/恢复；修复关闭回调重入。确认弹窗随主题统一，应用采用蓝底白 W 图标。
- 国家译名表与反应射击精确作用保持原状。仅中文更名不改对应英文术语。


## 1.7.1 — 2026-09-08

- 修复自动查找 Mod 重复列出同一目录：保存路径和 Steam 扫描路径统一为完整路径，去除尾分隔符，再忽略大小写去重。重复打开不会将三个 Mod 显示为六个。

## 1.7.0 — 2026-09-08

- 普通模式已显示字段可改，恢复独立弹药模块；栏位/角色简名统一到列表和筛选，师名与基础类型按确认版中文显示，特殊类型标签只在高级模式展示。
- 黑蓝改黑底亮蓝字；新增五套配色，共七套主题。设置支持图片背景、0–30% 透明度、填充/适应/平铺及持久化。
- 草稿支持多选、全选、部分应用和部分删除；批量草稿折叠成一批，展开选择子项。高级批改新增固定数值加减。
- 将军模式内置中英文营/连/组名称，完整营团为树根；新增 Pack 与索引窗口，数字索引仅高级模式编辑，继续共享隔离和跨文件校验事务。
- 单位与运输搜索加入基础小筛选，多选入口补全全选/清空；新增自动查找 Steam WARNO Mods，手动路径入口继续可用。
- 63/63 自动化测试通过；七主题、背景和普通/高级索引界面完成截图检查。

## 1.6.0 — 2026-09-08

- 新增将军模式：营列表、连排单位编制树、属性检查器；单位/数量/XP/运输，连排创建删除、跨父级移动、排序、HQ 和名称。
- 按结构开放战略棋子行动点、恢复、移动、战斗角色、控制区、支援范围、影响、名称与已有图标引用。
- 战略三文件、Pawn 与名称 CSV 接入统一草稿和事务；共享目标隔离、PackIndex 自动重算及候选回读校验。
- 新增设置：跟随系统 / 简体中文 / English、默认模式、双主题、版本与日志入口，切换不丢草稿。
- 普通轻度精简；高级提供结构/共享/复杂批改、引用工作台、字段搜索与受控原值编辑、分页源码前后对照。
- 60/60 自动化回归通过，覆盖战略共享隔离、索引增减、名称转义、换行与无 BOM、失败回滚、设置持久化和 WPF 导航/语言/草稿。
- 已有共享定义不做自动垃圾清理；图标使用现有引用。Unit 生命周期、战役地图/事件/剧情及资源制作仍不在范围内。


发布日期：2026-09-07

## 1.5.0 批量选择、运输校验与流畅预览

- 前置部署在普通模式同时提供可编辑数值和当前 Mod 动态预设；`2473.49823322` 与 `3533.56890459` 分别标注“侦察”“空降”，备注不会写入 NDF。
- “预览并应用全部”先让忙碌状态完成界面刷新，再在线程池重新索引、生成候选和校验；无变化字段不重复写草稿，Unit/Division 草稿不再无条件加载 Weapon/Ammo 工作区。全部应用前安全校验保持不变。
- 战术师正常跳过的其他用途 `TDeckDivisionDescriptor` 不再计入 Mod 问题，只在战术师页显示轻量兼容统计。
- 战术师运输新增可搜索复选面板，一次可选多种运输；新增候选必须在当前 Mod 中具有唯一 `TTransporterModuleDescriptor`，并在核心应用校验再次确认。既有无法确认的兼容值仍原样保留并标注。
- Unit 与 Weapon 复选列表常驻“全选当前筛选”“清空选择”和已选计数；程序化批量勾选只在完成后刷新一次。
- “生成 / 编译 Mod”移至顶栏，“打开 Mod 文件夹”移入始终可用的 Mod 工具菜单；主索引名称左边距从 10 调整为 14 DIP。
- 武器挂载的“使用的 Ammo”改用与大型 Weapon 列表相同的缓存、任意片段搜索、160 项限流和回收虚拟化选择器，提交仍只接受当前 Mod 中真实存在的 Ammo。
- Release 构建 0 警告、0 错误，57/57 自动化测试通过。测试未运行真实生成器、游戏、上传或官方备份脚本。

## 1.4.0 防呆表单、搜索与多选检查

- Unit 筛选改为原子类别，去掉多类别拼接产生的重复选项；新增“全部条件 / 任一条件”跨维度匹配切换，筛选按钮加大，Unit、Weapon、Ammo、战术师和对象列表首列增加左边距。
- Unit 类别、角色和标签改为标签化选择；战术师类型、标签同样改为标签式预设。常用值从当前 Mod 动态汇总，身份标签受保护，高级模式保留受校验的原始入口。
- 勾选至少两个 Unit 时增加第三栏批量检查器：共有值可直接修改，不同数值以 0 和“多种值”提示，最终仍通过批量预览、草稿和事务校验。乘系数操作默认向上取整。
- 缺失前置部署时可在严格的 UnitUpkeep → VisualShootPositions 锚点间补建模块，并列出当前 Mod 已有档位供参考；结构多义时继续只读降级。
- 新增护甲族、伤害族、物理伤害溅射和压制溅射字段；族选项与合法索引范围来自目标 Mod 自己的 `DamageResistance.ndf`，索引严格为 1…Count，步兵护甲族强制为 1。
- ECM 在界面显示正百分比，正式 NDF 保持负命中修正，零值固定写为 `0.0`；修订公路速度、燃油、视野/光学、战略值、精度和移动系数字段的中文名称及提示。
- Weapon 页的 Unit、Weapon、MountedWeapon 和替换候选，以及战术师新增 Unit/运输候选，均支持显示名或内部名任意片段搜索；大型列表使用预索引、结果上限和回收虚拟化以降低展开卡顿。
- 战术师运输改为可搜索的标签选择；费用曲线普通模式显示十个独立输入格，额外档位原样保留并只在高级模式处理。默认卡组编辑页移除，既有 Deck/DeckPack 数据保持不变，旧版相关草稿会明确冲突。
- Ammo 页增加可搜索的引用视图，按 Weapon 汇总其 Unit 引用者；展开区域限制高度，避免长引用链挤占字段编辑区。
- 修复合法 `Name is MAP [...]` 被顶层 NDF 扫描器误报为缺少左圆括号的问题；格式错误的 MAP 仍会报告未闭合方括号。
- Release 构建 0 警告、0 错误，55/55 自动化测试通过。测试未运行真实生成器、游戏、上传或官方备份脚本。

## 1.3.0 分组、标签筛选、问题中心与官方工具

- Unit、Weapon 和 Ammo 可编辑值按大类/小类分块，大类可折叠，字段卡随面板宽度排列；Unit 的“票数”更名为“将军模式价格”。
- Unit 筛选改为可呼出的多选标签面板；同维度 OR、跨维度 AND，收起后仅显示已选条件，每枚标签可单独移除。任何筛选变化会使旧批量预览失效。
- 新增“Mod 问题 / 工具问题”诊断中心，持久化日志包含错误编号、位置和详情；可一键复制诊断、打开日志目录，下次启动会提醒上次致命工具异常。
- “Mod 工具”只显示生成/编译、启动开发模式和上传三个目标 Mod 入口；三者按 BAT 和依赖独立探测，互斥运行并保留输出。界面不再单独暴露官方备份 BAT，编辑器自有事务备份不变。
- 新增 WARNO `Mods/CreateNewMod.bat` 入口：创建名称限 1–32 位 ASCII 英文字母/数字，拒绝 Windows 保留名和大小写不敏感重名；执行前检查官方依赖，完成后复核新 Mod 的 `GameData` 结构。
- Release 构建 0 警告、0 错误，51/51 自动化测试通过。官方命令的自动测试只使用系统临时目录中的假 BAT；未运行真实生成器、游戏、上传、官方备份或官方创建器。

## 1.2.0 筛选、草稿与 Ammo 编辑

- Unit 筛选行移除 Weapon 与 Ammo 两个下拉框，保留搜索、阵营、国家、单位类别、生产栏位、角色、所属师规则和草稿状态；每项上方增加 10 px 小字标签。
- Unit、通用对象和 Ammo 搜索框增加放大镜字形，搜索用途无需再依赖悬停提示判断。
- 左侧新增可点击的“草稿总览”页面，统一列出当前项目全部未应用草稿；点选后显示原值、目标值、模块、作用域、对象、字段、源文件和更新时间。
- “弹药”模块由只读对象索引升级为直接编辑页，可搜索并选择 Ammo，修改当前对象中已经存在且能唯一定位的字段，同时显示 Weapon/Unit 引用数量。
- 弹药页编辑的是共享 Ammo 本体，草稿明确使用“全部引用”作用域；需要只影响特定 Unit 时继续从武器页触发既有最小隔离链。
- 真实 WPF 回归覆盖筛选项与标签、三个搜索图标、草稿总览导航和详情、Ammo 直编草稿语义，并确认编辑阶段没有改写正式 NDF。

## 1.1.0 界面更新

- 增加参考深色科技界面的“黑蓝”主题和同结构“白蓝”主题；顶栏即时切换并保存选择，深色主题同步 Windows 原生标题栏。
- 强化通用对象索引的信息层级：名称使用较醒目的半粗体，内部类型、源文件和位置缩为次要小字，高级详情中的内部信息也保持次要层级。
- 对象索引默认按“源文件升序 → 文件内物理行号升序 → 字符偏移稳定兜底”排列；源文件和行号表头显示升序箭头，顶部文字明确说明当前排序，点击表头后同步更新。
- 项目栏、Unit 列表/检查器、Weapon 三栏、战术师列表/编辑器、对象索引/高级详情均增加可拖动分隔条，并为每栏限定最低可用宽度。
- WPF 回归扩展到主题、字号层级、排序链与箭头、分隔条和最小宽度、高级详情收起/恢复，以及 Unit/Weapon/战术师实际项目打开。
- 本次只调整界面呈现和布局，不改变 NDF/CSV 解析、草稿、正式写入、备份恢复或模块范围。

## 1.0.1 热修复

- 修复打开包含 Unit 模块的 Mod 后，批量选择数量等只读显示属性因 WPF 默认双向绑定而导致进程退出的问题；所有内联只读文本绑定已明确设为单向。
- 增加真实 WPF 主窗口项目打开回归，依次覆盖 Unit/Weapon 与完整战术师人工测试 Mod，防止仅验证空窗口启动而遗漏工作区绑定错误。
- 未改变 NDF/CSV 解析、草稿、正式写入或模块范围。

## 1.0.0 首版能力

- 从任意位置手动选择结构兼容的 WARNO Mod，并按单位、Weapon、Ammo 和战术师模块独立探测能力。
- 编辑 35 类 Unit 字段和游戏内名称；编辑现有 Weapon/Ammo 数值、引用和作用域，并在局部修改时建立最小共享隔离链。
- 编辑现有战术师的单位池、默认 Deck、激活费用和师级设置。
- 对已勾选 Unit 或当前完整筛选结果执行固定值、乘法、加法、增减百分比、三种取整和上下限批改。
- 批量执行前显示目标/命中/变化数量、样本和师/Weapon/Ammo 关联影响；确认后进入与单项编辑相同的可恢复草稿和统一事务。
- 正式应用前生成内存候选并校验，建立编辑器自有完整备份，通过同目录临时文件提交；支持失败回滚和人工恢复。
- 以 Windows x64 自包含便携 ZIP 发布，用户不需要安装 .NET SDK、Python、Node.js、数据库或外部 CLI。

## 已知边界

- 批量公式目前覆盖 Unit 字段；Weapon/Ammo 使用现有引用作用域编辑，批量方案不能保存成跨 Mod 预设。
- 1.0.x 不克隆或删除 Unit，不创建/复制/删除整师，不包含将军模式。
- 不自由新增/删除武器槽，不自动重建模型、贴图、动画等表现资源。
- 目标缺失或无法唯一定位某字段时只读降级，不猜测写入；具体原因可在字段提示和高级诊断中查看。
- 当前发布基线为 Windows x64 自包含 .NET 8；最低 Windows 版本仍待更多干净环境确认。
